{
  "name": "boop",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --configLoader native",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && node scripts/deployment-config.mjs && vite build --configLoader native && node scripts/validate-build.mjs",
    "preview": "vite preview --host 127.0.0.1 --configLoader native"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "typescript": "~5.9.3",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "@fontsource/poppins": "^5.2.6"
  }
}
